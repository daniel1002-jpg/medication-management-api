// import Treatment from '../../../../src/domain/entities/Treatment.js';

describe("Treatment Entity", () => {
  describe("Bussiness rules", () => {
    it("should reject a start date later than end date", () => {
      // const _data = {
      //     end_date: '2023-10-01',
      //     start_date: '2023-10-10'
      // };
      // expect(() => new Treatment(data)).toThrow('La fecha de inicio no puede ser posterior a la fecha de fin');
    });
  });
});
